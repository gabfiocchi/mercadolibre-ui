import { observable, computed, action } from 'mobx';

class Store {
  name = 'Mercado Libre';
  placheSearch = 'Nunca dejes de buscar';
  description = 'web ui Mercado Libre para test práctico';
}

export default Store;
